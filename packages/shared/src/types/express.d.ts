// Express augmentations are declared inline in:
// - middleware/correlation-id.ts (req.correlationId)
// - middleware/auth-jwt.ts (req.user)
// This file is intentionally left empty to avoid duplicate declarations.
